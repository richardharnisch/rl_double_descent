\section{Training Hyperparameters}\label{sec:appendix_hyperparameters}
\subsection{DQN}
We use a replay buffer size of 50,000 transitions and a target network update frequency of 5,000 training steps. The DQN is trained using the Adam optimizer \citep{kingma2017adammethodstochasticoptimization} with a learning rate of $10^{-3}$ and a batch size of 64. The exploration-exploitation trade-off is managed using an epsilon-greedy strategy, where the exploration rate $\epsilon$ decays linearly from 1.0 to 0.05 over the first 30\% of the episodes.
\subsection{TRPO}
The TRPO hyperparameters are a maximum KL divergence of $\delta=10^{-2}$, CG iterations set to 10 with damping 0.1, backtracking coefficient 0.5 for 10 line-search steps, GAE $\lambda=0.95$, and a batch size of 20 episodes per policy update. The value function is optimized for 5 iterations per update using Adam with learning rate $10^{-3}$.
\subsection{Training Runs}
We ran configurations of model type, train set size, number of training episodes, and environment configuration as shown in Tables~\ref{table:runs_capacity} and \ref{table:runs_episodic}.

\begin{table}[ht]
    \centering
    \setlength{\tabcolsep}{4pt}
    \begin{tabularx}{\columnwidth}{l|c|c|X}
        Type & Train Maps & Episodes  & Environment Configuration  \\
        \hline
        TRPO & 10         & 10,000    & Deterministic\footnotemark \\
        DQN  & 50         & 20,000    & Deterministic              \\
        DQN  & 100        & 50,000    & Deterministic              \\
        TRPO & 50         & 20,000    & Randomized                 \\
        TRPO & 100        & 50,000    & Randomized                 \\
        TRPO & 100        & 1,000,000 & Deterministic              \\
        TRPO & 500        & 1,000,000 & Deterministic              \\
        TRPO & 750        & 1,000,000 & Deterministic              \\
        TRPO & 1,000      & 1,000,000 & Deterministic              \\
    \end{tabularx}
    \caption{Capacity Regime Configurations}
    \label{table:runs_capacity}
\end{table}
\footnotetext{Agent starts in top left, goal in bottom right}
\begin{table}[ht]
    \centering
    \setlength{\tabcolsep}{4pt}
    \begin{tabularx}{\columnwidth}{l|c|X}
        Type & Train Maps & Environment Configuration \\
        \hline
        TRPO & 50         & Deterministic             \\
        TRPO & 100        & Deterministic             \\
    \end{tabularx}
    \caption{Episodic Regime Configurations}
    \label{table:runs_episodic}
\end{table}